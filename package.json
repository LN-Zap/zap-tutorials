{
  "name": "zap-tutorials",
  "version": "0.1.0",
  "description": "Tutorials and guides for Zap products.",
  "main": "index.js",
  "scripts": {
    "dev": "docz dev",
    "build": "docz build",
    "serve": "docz serve",
    "build:ci": "kill-port 3000 && (npm run dev &) && wait-on http-get://localhost:3000 && kill-port 3000 && npm run build",
    "clean": "rm -rf .docz",
    "deploy": "netlify deploy --dir=.docz/dist/ --prod"
  },
  "license": "MIT",
  "devDependencies": {
    "docz": "2.0.0-rc.38",
    "kill-port": "^1.5.1",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "wait-on": "^3.3.0"
  }
}
